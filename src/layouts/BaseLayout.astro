---
import '../styles/global.css';
import SiteHeader from '../components/astro/SiteHeader.astro';
import SiteFooter from '../components/astro/SiteFooter.astro';
import MobileActionBar from '../components/astro/MobileActionBar.astro';
import ConceptDisclosure from '../components/astro/ConceptDisclosure.astro';
import PortfolioBar from '../components/astro/PortfolioBar.astro';
import EnquiryDrawer from '../components/react/EnquiryDrawer';
import CartProvider from '../components/react/CartProvider';
import AssistantChat from '../components/react/AssistantChat';
import { getPackagesUrl, STORE } from '../lib/config';
import { robotsMetaContent } from '../lib/demo-mode';
import { absoluteUrl, buildTitle, jsonLdScript } from '../lib/seo';

interface Props {
  title?: string;
  description?: string;
  path?: string;
  ogImage?: string;
  schemas?: unknown[];
  showDisclosure?: boolean;
}

const {
  title,
  description = `${STORE.name} is a modern Canadian home and lifestyle store concept by Che Xu Studio—featuring curated collections, product discovery, wishlist, demo cart and checkout.`,
  path = Astro.url.pathname,
  ogImage = '/og/default.svg',
  schemas = [],
  showDisclosure = true,
} = Astro.props;

const pageTitle = buildTitle(title);
const canonical = absoluteUrl(path);
const og = absoluteUrl(ogImage.endsWith('/') ? ogImage.slice(0, -1) : ogImage).replace(/\/$/, '');
const robots = robotsMetaContent();
const turnstileSiteKey = import.meta.env.PUBLIC_TURNSTILE_SITE_KEY;
const packagesUrl = getPackagesUrl();
---

<!doctype html>
<html lang="en-CA">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="robots" content={robots} />
    <meta name="description" content={description} />
    <meta name="generator" content={Astro.generator} />
    <link rel="canonical" href={canonical} />
    <link rel="icon" type="image/svg+xml" href="/brand/hp-monogram.svg" />
    {
      path === '/' && (
        <link rel="preload" as="image" href="/images/hero-home.svg" fetchpriority="high" />
      )
    }
    <meta property="og:type" content="website" />
    <meta property="og:locale" content="en_CA" />
    <meta property="og:site_name" content={STORE.name} />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={og} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={pageTitle} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={og} />
    <title>{pageTitle}</title>
    {
      schemas.length > 0 && (
        <script is:inline type="application/ld+json" set:html={jsonLdScript(schemas)} />
      )
    }
  </head>
  <body class="mobile-action-pad">
    <a class="skip-link" href="#main">Skip to content</a>
    <PortfolioBar />
    <SiteHeader />
    {
      showDisclosure && (
        <div class="container-page disclosure-wrap">
          <ConceptDisclosure compact />
        </div>
      )
    }
    <main id="main">
      <slot />
    </main>
    <SiteFooter />
    <MobileActionBar />
    <CartProvider client:idle />
    <EnquiryDrawer client:idle packagesUrl={packagesUrl} turnstileSiteKey={turnstileSiteKey} />
    <AssistantChat client:visible />
    <script is:inline>
      window.dispatchEvent(
        new CustomEvent('hp:analytics', {
          detail: { event: 'demo_viewed', payload: { path: location.pathname }, at: Date.now() },
        }),
      );
    </script>
  </body>
</html>

<style>
  .disclosure-wrap {
    padding-top: 0.85rem;
  }
</style>
